[package]
name = "bplus_tree"
version = "0.1.0"
edition = "2024"

[features]
default = []
serde = ["serde"]        # Enable serde support when the `serde` feature is enabled
[dependencies]
bincode = { version = "1.3", optional = true } # Optional dependency for serialization
